<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cminnovation.backend.board.mapper.PostMapper">
	
	<!--insert 문 PK 자동생성-->
	<insert id="insert" parameterType="Post" useGeneratedKeys="true" keyProperty="id">
		INSERT INTO posts (title, content, author, views, created_at, updated_at)
		VALUES (#{title}, #{content}, #{author}, 0, NOW(), NOW())
	</insert>
	
	<!--조회-->
	<select id="findById">
		SELECT *
		FROM posts
		WHERE id = #{id}
	</select>
	
	<!--전체 조회-->
	<select id="findAll">
		SELECT *
		FROM posts
		ORDER BY created_at DESC
	</select>
	
	<!--수정-->
	<update id="update">
		UPDATE posts
		SET title = #{title},
		    content = #{content},
		    author = #{author},
		    updated_at = NOW()
		WHERE id = #{id}
	</update>
	
	<!--삭제-->
	<delete id="delete">
		DELETE 
		FROM posts 
		WHERE id = #{id}
	</delete>
	
	<!--조회수-->
	<update id="increaseViews" parameterType="long">
		UPDATE posts
		SET views = views + 1
		WHERE id = #{id}
	</update>
		
</mapper>